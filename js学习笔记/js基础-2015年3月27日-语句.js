(function () {
    
    /*
        if语句
        if(a){}else if(b){}else{}
        如果a成立执行第一个语句块下的否则如果b成立执行第二个语句块下的，否则执行最后一个语句块

        do-while语句
        do{}while(a)
        先执行一次do语句块，接着验证a是否成立，如果成立继续执行do语句块，否则不执行

        while语句
        while(a){}
        先验证a是否成立，如果成立执行语句块，否则不执行

        for语句
        for(a;b;c){}
        首先定义a，在验证b是否成立，如果成立后执行语句块，再执行c。如果b不成立则不执行

        for in 语句
        for(a in b){}
        循环将b中的属性赋值给a，接着执行语句块。知道b中的属性遍历完为止

        label语句
        label : a
        定义后由break或continue语句引用。

        break和continue语句
        用于跳出循环

        with语句
        with(window.location){}
        with语句用户将代码的作用域设置到一个特定的对象中。在with语句块中可以直接使用with所引用对象的属性和方法
        使用with语句会是作用域链增长，将临时的对象扩展到作用域链的头

        switch语句
        switch(a){case b: break case c: break}
        分支语句。如果a和某个case中的表达式相等，则执行相应的语句块。break用于跳出switch。
    */

})();